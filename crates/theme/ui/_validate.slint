// Stub Window so slint-build has something to compile and CI catches
// any syntax error in theme.slint. Not exported to consumers.
import { Theme } from "theme.slint";

export component _ThemeValidate inherits Window {
    background: Theme.background;
    width: 1px;
    height: 1px;
}
