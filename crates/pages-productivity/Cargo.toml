[package]
name = "slint-mobile-pages-productivity"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Page templates (productivity category) for slint-mobile-components."
publish = false

[lib]
crate-type = ["rlib"]
