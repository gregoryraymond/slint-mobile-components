[package]
name = "slint-mobile-pages-travel"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Page templates (travel category) for slint-mobile-components."
publish = false

[lib]
crate-type = ["rlib"]
