// Stub Window that imports every public component so slint-build
// type-checks the whole crate at compile time.
import { Theme } from "@mobile-theme/theme.slint";
import { AppBar } from "app-bar.slint";
import { Avatar } from "avatar.slint";
import { Badge } from "badge.slint";
import { Banner } from "banner.slint";
import { BottomBar } from "bottom-bar.slint";
import { BottomNav } from "bottom-nav.slint";
import { BottomSheet } from "bottom-sheet.slint";
import { MobileButton, TextButton, Fab } from "button.slint";
import { Card } from "card.slint";
import { Checkbox } from "checkbox.slint";
import { Chip } from "chip.slint";
import { ContactRow } from "contact-row.slint";
import { Dialog } from "dialog.slint";
import { Divider } from "divider.slint";
import { DotIndicator } from "dot-indicator.slint";
import { Drawer } from "drawer.slint";
import { EmptyState } from "empty-state.slint";
import { IconButton } from "icon-button.slint";
import { FormField } from "form-field.slint";
import { FormSection } from "form-section.slint";
import { KeyValueRow } from "key-value-row.slint";
import { LeadingIconTile } from "leading-icon-tile.slint";
import { ListItem } from "list-item.slint";
import { MetaRow, MetaChip } from "meta-row.slint";
import { ProgressBar } from "progress-bar.slint";
import { SettingRow } from "setting-row.slint";
import { StatTile } from "stat-tile.slint";
import { Radio } from "radio.slint";
import { SectionHeader } from "section-header.slint";
import { SegmentedControl } from "segmented-control.slint";
import { Skeleton } from "skeleton.slint";
import { Slider } from "slider.slint";
import { Snackbar } from "snackbar.slint";
import { Spinner } from "spinner.slint";
import { Stepper } from "stepper.slint";
import { MobileSwitch } from "switch.slint";
import { TabBar, Tab } from "tab-bar.slint";
import { TextField } from "text-field.slint";

export component _ComponentsValidate inherits Window {
    background: Theme.background;
    width: 1px;
    height: 1px;
}
