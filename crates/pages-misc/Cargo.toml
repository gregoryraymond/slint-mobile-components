[package]
name = "slint-mobile-pages-misc"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Page templates (misc category) for slint-mobile-components."
publish = false

[lib]
crate-type = ["rlib"]
