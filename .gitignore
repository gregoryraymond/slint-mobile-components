/target
**/*.rs.bk
Cargo.lock.bak

# Maestro drops screenshots from `takeScreenshot` and `assertScreenshot`
# failure diffs into the cwd. Committed baselines live under
# `maestro/flows/baselines/`; everything else is ephemeral.
/*.png
/maestro-debug/

# Editors
.idea/
.vscode/
*.swp
.DS_Store
