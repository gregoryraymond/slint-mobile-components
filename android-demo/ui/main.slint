// Android demo entry point. The APK ships one screen — HomePage — as
// proof the workspace compiles end-to-end for the Android target. The
// real "browse the screen library" experience is the desktop viewer
// (`cargo run`); on a phone, embed a screen directly into your own
// app the same way this file does.

import { HomePage } from "@mobile-pages-misc/home.slint";

export component MainWindow inherits HomePage {}
