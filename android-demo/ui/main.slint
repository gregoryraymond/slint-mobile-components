// Android demo entry point — wraps the components-library Gallery so
// `slint::include_modules!()` in src/lib.rs has a `MainWindow` to
// instantiate from `android_main`. The Gallery itself lives in the
// components crate (resolved through `@mobile-components/`).

import { Gallery } from "@mobile-components/gallery.slint";

export component MainWindow inherits Gallery {}
