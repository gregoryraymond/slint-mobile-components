# Maestro workspace config.
# https://docs.maestro.dev/cli/configuration
#
# The flows/ directory is the test root. Baseline PNGs for
# `assertScreenshot` live under `flows/baselines/` and are committed
# to the repo as the visual contract for the on-device E2E.

flows: flows
