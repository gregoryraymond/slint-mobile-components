# `cargo xtask <subcommand>` — runs the workspace tooling crate.
# Defined here rather than as a separate shell wrapper so it works
# uniformly on macOS, Linux, and Windows.
[alias]
xtask = "run --package xtask --"
